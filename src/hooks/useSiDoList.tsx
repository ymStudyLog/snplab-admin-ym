import React from 'react';
//import { regionService, getRegionData } from '../../api/api';

type Props = {};

const useSiDoList = (props: Props) => {
  // const [temp, setTemp] = React.useState<string>('기본값');
  //console.log(temp);
  // React.useEffect(() => {
  //   getRegionData(regionService).then((data) => {
  //     setTemp(data);
  //   });
  // }, []);
  // console.log(temp);

  return <div>useSiDoList</div>;
};

export default useSiDoList;
